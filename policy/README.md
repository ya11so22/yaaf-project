# /policy

Policy-as-code guardrails for Phase 2: OPA/`conftest` checks run in CI against Terraform plans,
plus Kubernetes manifest linting — guardrails without needing an always-on admission controller.

Empty until Phase 2 starts.
