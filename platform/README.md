# /platform

Platform-layer tooling and docs for Phase 2: ephemeral PR environment lifecycle (spin up on PR
open, tear down on PR close/idle), and — as a stretch goal — a minimal service catalog page in
place of standing up Backstage full-time.

Empty until Phase 2 starts.
